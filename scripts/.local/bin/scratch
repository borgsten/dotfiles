#! /usr/bin/env bash

# Until all systems are migrated
if command -v uwsm >/dev/null 2>&1; then
    uwsm app -- ghostty --class=com.scratch -e tmux &
else
    alacritty --class term_scratchpad -e tmux &
fi
